---
interface Props {
  name: string;
  slug: string;
}

const { name, slug } = Astro.props;
---

<a
  href={`/blog/categoria/${slug}`}
  class="inline-block bg-accent-gold text-black px-4 py-2 rounded-full text-sm font-semibold hover:bg-yellow-500 transition-colors"
>
  {name}
</a>
